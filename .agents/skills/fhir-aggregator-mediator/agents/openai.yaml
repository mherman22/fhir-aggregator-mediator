interface:
  display_name: "Fhir Aggregator Mediator"
  short_description: "Repo-specific patterns and workflows for fhir-aggregator-mediator"
  default_prompt: "Use the fhir-aggregator-mediator repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true